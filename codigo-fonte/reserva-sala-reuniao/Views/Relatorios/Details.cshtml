@model reserva_sala_reuniao.Models.Relatorio

@{
    ViewData["Title"] = "Details";
}

<div class="container h-100">
    <div class="row h-100 justify-content-center align-items-center">
        <div class="col-md-6">
            <div class="card shadow-lg p-4 mb-5 bg-white rounded">
                <div class="card-body">
                    <h1 class="text-center">Exibir</h1>
                    <h4 class="text-center">Relatorios</h4>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-4"< >Tipos de relatorio</>                            
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.TipoRelatorioId)
                        </dd>
                        <dt class="col-sm-4">
                            @Html.DisplayNameFor(model => model.Arquivo)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.Arquivo)
                        </dd>
                    </dl>
                </div>
                <div class="text-center mt-3">
                    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary">Editar</a>
                    <a asp-action="Index" class="btn btn-link">Voltar</a>
                </div>
            </div>
            
        </div>
    </div>
</div>
